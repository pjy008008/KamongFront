import Nav from "../Nav";
import styled from "styled-components";
const contentsContainer = styled.div`

`
const Edit = () => {
  return (
    <div>
      <Nav />
      여기서 관리자 권한으로 수정 가능
    </div>
  );
};
export default Edit;
